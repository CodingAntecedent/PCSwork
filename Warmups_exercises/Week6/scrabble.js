// create an array of hashes, key is letters, do an each do over each key with regex
// if there is a match then pull the value and push it into a score array


var scrabble =  function(input) {
  var score = {
    // store the scores in here
  };

  var split = function(word) {
    // break up the word in here
  };




};